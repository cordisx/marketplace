# Official publisher records

Store one `marketplace-official.v1` JSON record per plugin. A record may remain
active across versions only while its plugin id, canonical CordisX source, npm
publisher identity, package namespace, and package name remain exact.
